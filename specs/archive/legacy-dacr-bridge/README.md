# Legacy DACR Bridge Spec

The materialized DACR/dataset_v2 bridge was rejected by
`docs/CORETEX_SOURCE_DATA_AUDIT.md` because the dataset export does not carry
retrieval-shaped graded qrels, hard-negative pools, temporal labels, or
multi-hop relation labels.

The launch corpus is generated from the coordinator challenge library using
synthesizer-category qrels.
